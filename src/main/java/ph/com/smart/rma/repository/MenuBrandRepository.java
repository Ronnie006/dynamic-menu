package ph.com.smart.rma.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import ph.com.smart.rma.model.MenuBrand;

public interface MenuBrandRepository extends JpaRepository<MenuBrand, Long>{
	
}
